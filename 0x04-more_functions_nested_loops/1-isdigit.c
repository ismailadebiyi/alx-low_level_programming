#include "main.h"
#include <ctype.h>
/**
 * _isdigit - method to check if a given paramater is a digit
 * @c: parameter to check.
 * Return: 1 if true or 0 if false
 */
int _isupper(int c)
{
	if (isdigit(c))
	{
		return (1);
	}
		return (0);
}
