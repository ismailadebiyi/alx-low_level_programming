#include "main.h"
#include <ctype.h>
/**
 * _isupper - method to check if a given letter is in upper case
 * @c: letter to check.
 * Return: 1 if true or 0 if false
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
		return (0);
}
